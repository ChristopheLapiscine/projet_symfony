{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'fos_user_profile_edit' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
<button class="btn btn-primary"><a href="{{ path('fos_user_profile_show') }}">Profil</a></button>
<button class="btn btn-primary"><a href="{{ path('home') }}">Aller au site</a></button>
{{ form_end(form) }}
